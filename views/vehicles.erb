<ul>
  <% if Vehicle.length() == 0 %>
    There are no vehicles on the lot at this time : (
  <% else %>
    <% @vehicles.each() do |vehicle| %>
      <%= vehicle.make() %>, <%= vehicle.model() %>, <%= vehicle.year() %> 
    <% end %>
  <% end %>
</ul>
